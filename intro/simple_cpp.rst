.. include:: ../links.txt
.. include:: ../roles.txt

.. _intro_simple_cpp:

Simple `C++`_
=============

.. contents:: Table of Contents
   :local:
   :backlinks: top

The ``main`` functions
----------------------

Each `C++`_ program is written in a ``main`` function in `C++`_, where a
``main`` function must return an integer to indicate your system the exit
status of the program.

.. note:: 0 is used for exiting successfully.

Here is the simplest `C++`_ program, :cpp:`int main() { return 0; }`
